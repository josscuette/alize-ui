/**
 * Alize Component Library
 * 
 * This file re-exports everything from src/index.ts for backward compatibility.
 * The actual source is in /src/.
 * 
 * @example
 * ```tsx
 * import { Button, Input, Card } from 'alize'
 * ```
 */

// Re-export everything from src/index.ts
export * from './src/index'
