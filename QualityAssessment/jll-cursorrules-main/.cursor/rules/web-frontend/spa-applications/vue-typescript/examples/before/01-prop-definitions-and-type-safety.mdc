---
description: Props Definitions and Type Safety Before Example
globs: [],
alwaysApply: false,
---

```vue
<!-- ❌ BAD EXAMPLE - Violates Prop Definitions and Type Safety Rules -->
<template>
  <div class="user-card">
    <h3>{{ user.name || 'Unknown' }}</h3>
    <p>Status: {{ status }}</p>
    <p>Count: {{ count }}</p>
    <button @click="updateUser">Update</button>
  </div>
</template>

<script setup lang="ts">
// ❌ BAD: No type safety or validation - using array syntax
const props = defineProps(['status', 'user', 'count']);

// ❌ BAD: No type checking for emits
const emit = defineEmits(['update', 'delete']);

// ❌ BAD: No type safety - any type used
const updateUser = () => {
  // This could break at runtime if user doesn't have expected properties
  emit('update', {
    id: props.user.id, // Could be undefined
    name: props.user.name, // Could be undefined
    email: props.user.email // Could be undefined
  });
};

// ❌ BAD: No validation means invalid data can be passed
// Component could receive:
// - status: 123 (number instead of string)
// - user: null (null instead of object)
// - count: "invalid" (string instead of number)
</script>

<style>
/* ❌ BAD: Global styles that could conflict */
.user-card {
  padding: 1rem;
  border: 1px solid #ccc;
}

h3 {
  color: blue; /* Could affect other components */
}

button {
  background: red; /* Global button styling */
}
</style>
```